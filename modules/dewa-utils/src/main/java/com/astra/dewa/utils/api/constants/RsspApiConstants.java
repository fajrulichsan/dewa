package com.astra.dewa.utils.api.constants;

import com.astra.dewa.utils.api.DealinkApiUtil;

/**
 * @author psmfajru1107
 */
public class RsspApiConstants {
    public static final String URL_RSSP = DealinkApiUtil.getCredential("rssp", "URL_RSSP");
    public static final String CLIENT_ID_RSSP = DealinkApiUtil.getCredential("rssp", "CLIENT_ID_RSSP");
    public static final String CLIENT_SECRET_RSSP = DealinkApiUtil.getCredential("rssp", "CLIENT_SECRET_RSSP");
}
